interface:
  display_name: "Token Receipt"
  short_description: "把 token 用量打印成 ASCII 小票"
  brand_color: "#111111"
  default_prompt: "Use $token-receipt to print a shareable ASCII receipt for this conversation's token usage."

policy:
  allow_implicit_invocation: true

